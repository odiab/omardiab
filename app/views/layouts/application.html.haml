!!!
%html
  %head
  %link{href: 'http://fonts.googleapis.com/css?family=Open+Sans:300,400,600', rel: 'stylesheet', type: 'text/css'}
    %title Omar Diab
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application", "welcome"
    = @headtags
    = csrf_meta_tags
  %body
    #header
      %span#banner= link_to("Omar Diab", 'home#index', id: 'bannerText');
      %ul.nav
        -Omardiab::Application.config.sections.each do |section|
          -if section != @curSection
            %li.section
              = link_to(section, section)
    #content
      = yield
    #footer
      %p
        Omar Diab &copy; 2012 &#124; 
        = link_to('About this site', 'about')
